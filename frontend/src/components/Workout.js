import React, { useEffect } from "react";
import useAxios from "../utils/UseAxios";

const Workout = () => {

    return(
        <div>
            <h1>Workout</h1>
        </div>
    );
};

export default Workout;
